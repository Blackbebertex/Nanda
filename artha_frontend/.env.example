# Required for Vercel production build (Settings → Environment Variables)
BACKEND_URL=https://artha-api-lsli.onrender.com

# Local dev: npm run build  (without this file) defaults to http://localhost:8000
